{
  "name": "agentbus",
  "version": "0.1.0",
  "description": "AgentBus CLI/Daemon —— MQTT 总线客户端（架构见 ../ARCHITECTURE.md）",
  "type": "module",
  "bin": {
    "agentbus": "dist/cli.js"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
